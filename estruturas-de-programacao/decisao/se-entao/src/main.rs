//Estrutura de decisão se-entao

fn main() {
    let a: i32 = 10;

    if a < 100 {
        println!("A variável é válida")
    }
}
    // Caso seja verdadeira a expressão será exibido a mensagem.
    // Neste exemplo não foi declarado um condição então, esta é opcional.
